import { Container } from '@chakra-ui/react';
import React from 'react';

const Footer: React.FC = () => {
	return (
		<Container maxW="container.lg" height={200} mt={50}>
			푸터
		</Container>
	);
};

export default Footer;
