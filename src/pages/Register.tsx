import { Box, Container } from '@chakra-ui/react';
import React from 'react';

const Register = () => {
	return (
		<Container maxW="container.lg" bg="red" centerContent>
			sdf
		</Container>
	);
};

export default Register;
